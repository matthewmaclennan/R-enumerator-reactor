#Mode refers to the following:
#Enumerate the combinations of atoms
atomcombo<-function(atomlist,length,mode=,){
  atomlist<-as.character(atomlist)
  finallength<-as.numeric(finallength)
  
  do.call(paste0,expand.grid())
  
}
